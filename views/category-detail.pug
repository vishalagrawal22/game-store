extends ./layout.pug

block styles 
  link(rel="stylesheet", href="/stylesheets/game-card.css")

block content 
  .p-3.pb-0
    h2= category.name
    p= category.description
    a.btn.btn-primary.me-3(href=`${category.url}/update`) Update category
    a.btn.btn-danger(href=`${category.url}/delete`) Delete category
    h4.mb-3.mt-3 Games in the category
  hr
  include includes/game-list-filter.pug  
  hr
  include includes/game-card-list.pug