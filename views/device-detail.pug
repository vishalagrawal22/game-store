extends ./layout.pug

block content 
  div(style="padding: 16px; padding-bottom: 0px;")
    h2= device.name
    a.btn.btn-primary(href=`${device.url}/update`, style="color: white; margin-right: 16px") Update device
    a.btn.btn-danger(href=`${device.url}/delete`, style="color: white;") Delete device
    h4(style="margin-top: 16px;") Compatible Games
  include includes/game-card-list.pug